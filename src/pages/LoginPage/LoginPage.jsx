import { LoginForm } from 'components/LoginForm/LoginForm';

export default function LoginPage() {
  return (
    <div
      style={{
        margin: '0 auto',
        padding: '25px 16px',
        fontSize: '25px',
        fontWeight: '500',
      }}
    >
      <LoginForm />
    </div>
  );
}
